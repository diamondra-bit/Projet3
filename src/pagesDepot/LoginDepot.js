import { useState } from 'react'
import './cssDepot/LoginDepot.css'

function LoginDepot(){

    /*const [dark,setDark] = useState(false);
    const [numero,setNumero] = useState('');

    const changeToDark = () => {
        setDark(!dark);
    }
    const handleChange = (e) => {
        setNumero(e.target.value)
    }

    return(
        <>
            <div className="block shadow">
                <input className='form-control' type='text' placeholder='numéro' value={numero} onChange={handleChange}/>
                <input className='form-control' type='text' placeholder='mot de passe'/>
                <button className={dark ? 'btn btn-primary' : 'btn btn-dark'} onClick={changeToDark}>Dark</button>
            </div>
        </>
    )*/
}
export default LoginDepot